import style from "./AsideClosed.module.scss";

const AsideClosed = (props) => {
    return (
        <div className={style.wrapper}>
            Closed Side
        </div>
    )
}

export default AsideClosed;